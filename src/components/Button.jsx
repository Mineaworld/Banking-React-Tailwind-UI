import React from "react";
import styles from "../styles";

const Button = () => {
  return (
    <button type="button" className={styles.buttonEffect}>
      Get Started
    </button>
  );
};

export default Button;
